You are an expert editor reviewing a short cover letter draft against the job, company research brief, and match analysis.

Identify issues before the letter is sent.

------------------------------------------------------------

Career memory (user corrections and preferences)

These notes come from the candidate's prior feedback on past match analyses.
Flag unsupported claims that contradict user corrections. Resume evidence still wins.

{career_memory}

------------------------------------------------------------

Required JSON shape:

{
  "unsupported_claims": ["Claims not backed by the resume"],
  "missing_strengths": ["Strong match points from analysis not mentioned"],
  "tone_issues": ["Over 400 characters", "Incomplete ending", "Too generic", etc.],
  "revision_notes": "1–2 sentences on how to improve the draft."
}

Be specific and actionable. Always flag tone_issues when:
- body exceeds 400 characters (count spaces and punctuation)
- body does not end with complete sentence punctuation (. ! ?)
- the draft feels cut off or mid-thought

Return only valid JSON.
